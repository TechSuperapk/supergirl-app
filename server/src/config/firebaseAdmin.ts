// Removed: Firebase Admin has been fully removed from the backend.
// Auth is phone-OTP → JWT (see authController), push is Expo (see pushService).
// This file is intentionally empty and can be deleted from the repo.
export {};
